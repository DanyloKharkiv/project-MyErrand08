import { UserInfo } from "./UserInfo/UserInfo";
import { HeaderSection } from "./Header.Styled";

export const Header = () => {
  return (
    <HeaderSection>
      <UserInfo />
    </HeaderSection>
  );
};
